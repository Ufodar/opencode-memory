# 研究记录：Work Investment Spent Phrasing

## `claude-mem` 对照

- `claude-mem` 的 `Work investment` 同位置强调这些 token 是已经花在 research、building、decisions 上的过去投入
- 当前 `opencode-memory` 同位置还是 `captured from prior work`

## 结论

这不是缺少大模块，而是 `Work investment` 的同一行 wording 还不够像 `claude-mem`。  
本轮只补这一处 phrasing。
