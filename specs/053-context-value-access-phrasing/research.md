# Research: Context Value Access Phrasing

## 对照结论

当前 `opencode-memory` 的 `[CONTEXT VALUE]` 已经有一条 access line，但 phrasing 还是 `prior work`。

本地 `claude-mem` 在 footer 同位置会明确给出 `past research & decisions`。

结合当前仓已在 `[TOKEN KEY]` 中补过 `research / building / deciding`，这轮只把 footer access line phrasing 对齐到同样更具体的语义。
