# Data Model：Memory Search Guidance

本轮不改数据库 schema。

只调整一个 deterministic header line：

- `memory_search=broader lookup`

目标改成更具体的用途说明，例如：

- `memory_search=past decisions, bugs, deeper research`

影响范围仅限：

- system context header
- 相关测试断言
