# Quickstart: Next Steps Snapshot Label

1. 运行 `bun test tests/runtime/system-context.test.ts tests/runtime/compaction-context.test.ts`
2. 确认 snapshot 断言现在要求 `Next Steps:`
3. 实现最小改动
4. 重新运行 targeted tests
5. 运行 `bun test && bun run typecheck && bun run build`
