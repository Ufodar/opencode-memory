# Research: Next Steps Snapshot Label

## 对照结论

当前 `opencode-memory` 的 snapshot 已经有完整 next action 字段，但 label 还是 `Next`。

本地 `claude-mem` 的 summary 同位置使用 `Next Steps`。

这轮只改 label，不改 snapshot 内容和 timeline summary child line。
