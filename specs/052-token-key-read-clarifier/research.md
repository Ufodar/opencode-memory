# Research: Token Key Read Clarifier

## 对照结论

当前 `opencode-memory` 已经有 `[TOKEN KEY]`，而且 `Work` clarifier 已对齐。

本地 `claude-mem` 在同位置还会明确给出 `Read` 是 `cost to learn it now`。

## 本轮范围

只补这一个 clarifier：

- system context 的 `Read` line 加上 `(cost to learn it now)`
- compaction context 继续不出现 `[TOKEN KEY]`

不改其他 section。
