# Quickstart：Savings Reduction From Reuse

1. 运行 system-context targeted tests，先看到 `Your savings` 新断言失败。
2. 修改 economics 文本构造逻辑，把 `reuse reduction` 改成 `reduction from reuse`。
3. 重新运行 targeted tests，确认其他 economics 行不变。
