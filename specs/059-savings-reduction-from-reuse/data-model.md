# 数据模型：Savings Reduction From Reuse

## 影响对象

- `ContextEconomicsLines`

## 变更

- `Your savings` 行的百分比 phrasing 改成 `reduction from reuse`

## 不变

- counts line 保持不变
- `Loading` 保持不变
- `Work investment` 保持不变
- compaction context 继续不显示 economics
