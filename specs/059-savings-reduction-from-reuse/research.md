# 研究记录：Savings Reduction From Reuse

## `claude-mem` 对照

- `claude-mem` 的 `Your savings` 同位置把百分比写成 `reduction from reuse`
- 当前 `opencode-memory` 同位置还是 `reuse reduction`

## 结论

这不是缺少大模块，而是 `Your savings` 同一行的百分比 phrasing 还没对齐。  
本轮只补这一处 phrasing。
