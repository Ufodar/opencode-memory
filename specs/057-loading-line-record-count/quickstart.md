# Quickstart：Loading Line Record Count

1. 运行 system-context targeted tests，先看到对 `Loading` 行记录数的新断言失败。
2. 修改 economics 文本构造逻辑，让 `Loading` 行输出 `N records (~Xt to read)`。
3. 重新运行 targeted tests，确认 counts / work / savings 没被误改。
