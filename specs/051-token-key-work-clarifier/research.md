# Research: Token Key Work Clarifier

## 对照结论

当前 `opencode-memory` 已经有 `[TOKEN KEY]`，但 `Work` line 还是泛化表述。

本地 `claude-mem` 在同位置会明确给出 `research / building / deciding` 作为历史工作投入的例子。

## 本轮范围

只补这一个 clarifier：

- system context 的 `Work` line 加上 `(research, building, deciding)`
- compaction context 继续不出现 `[TOKEN KEY]`

不改其他 section。
