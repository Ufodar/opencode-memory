# Quickstart: Observation Token Hints

## 目标

让 expanded observation 除了 `Result / Tool / Evidence` 外，还能直接显示局部 `Read / Work` token hint。

## 验证命令

```bash
cd /Users/storm/Documents/code/study_in_happy/projects/opencode-memory
bun test tests/runtime/system-context.test.ts tests/runtime/compaction-context.test.ts
bun test
bun run typecheck
bun run build
```
