# Data Model: Loading Observations Wording

本轮不新增数据模型，不改输入结构。

现有输入继续使用：

- `summaryCount`
- `directObservationCount`
- `coveredObservationCount`
- `loadingTokens`

唯一变化是输出文案从 `records` 改成 `observations`。
