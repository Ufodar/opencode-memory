# Quickstart: Loading Observations Wording

1. 运行：
   - `bun test tests/runtime/system-context.test.ts`
2. 确认 `Loading` 行显示：
   - `Loading: N observations`
3. 再运行：
   - `bun run typecheck`
   - `bun run build`
